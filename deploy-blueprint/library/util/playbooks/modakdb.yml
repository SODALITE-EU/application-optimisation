---
- hosts: all
  become: no

  tasks:
    - name: Install sample database
      copy:
        dest: "{{ path }}"
        src: modakdb.sql
    - name: Change permissions    
      file:
        path: "{{ path }}"
        mode: 0777        