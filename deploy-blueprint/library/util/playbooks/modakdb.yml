---
- hosts: all
  become: no

  tasks:
    - name: Install sample site
      copy:
        dest: "{{ path }}"
        src: modakdb.sql
    - name: Change permissions    
      file:
        path: "{{ path }}"
        mode: 0777        